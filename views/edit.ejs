<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/css/add.css">
</head>
<body>


     <div class="container">
        <h1>🎬 Edit Movie</h1>

        <form action="/editmovie/<%= movie._id %>" method="POST" enctype="multipart/form-data">
            <div class="form-group">
                <label for="name">Movie Name</label>
                <input type="text" id="name" name="name" placeholder="Enter movie title..." value="<%= movie.name %>" required>
            </div>

            <div class="form-group">
                <label for="actor">Lead Actor</label>
                <input type="text" id="actor" name="actor" placeholder="Enter main actor's name..." value="<%= movie.actor %>" required>
            </div>

            <div class="form-group">
                <label for="movieType">Genre</label>
                <input type="text" id="movieType" name="movieType" placeholder="e.g., Action, Drama, Comedy..." value="<%= movie.movieType %>" required>
            </div>

            <div class="form-group">
                <label for="shortDetails">Short Description</label>
                <input type="text" id="shortDetails" name="shortDetails" placeholder="Brief movie summary..." value="<%= movie.shortDetails %>" required>
            </div>

            <div class="form-group">
                <label for="fullDetails">Full Details</label>
                <textarea id="fullDetails" name="fullDetails" placeholder="Complete movie description, plot, cast details..." required><%= movie.fullDetails %></textarea>
            </div>

            <div class="form-group">
                <label for="img">Movie Poster</label>
                <div class="file-input-wrapper">
                    <input type="file" id="img" name="img" accept="image/*" required>
                    <label for="img" class="file-input-label">
                        <span>Choose movie poster image</span>
                    </label>
                </div>
            </div>

            <button type="submit" class="submit-btn">
                Add Movie to Database
            </button>
        </form>
    </div>

</body>
</html>